use testdb

--테이블생성
CREATE TABLE TB_BOARD
(
	IDX INT,
	TITLE VARCHAR(100),
	CONTENT VARCHAR(MAX),
	WRITER VARCHAR(20),
	WRITE_DATE DATETIME
)

--테이블조회
SELECT
	IDX, TITLE, CONTENT, WRITER, WRITE_DATE
FROM
	TB_BOARD WITH(NOLOCK)